apiVersion: v1
kind: Secret
metadata: 
  name: {{ include "dstack.fullname" . }}
  labels:
    {{- include "dstack.labels" . | nindent 4 }}
type: Opaque
data: 
  private_key.pem: {{ .Values.private_key | b64enc }}
