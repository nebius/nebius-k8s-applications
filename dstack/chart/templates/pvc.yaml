apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "dstack.fullname" . }}
  labels:
    {{- include "dstack.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storage_size }}
