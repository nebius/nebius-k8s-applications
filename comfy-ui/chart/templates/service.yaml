apiVersion: v1
kind: Service
metadata:
  name: comfyui
  annotations:
    nebius.com/MarketplaceServiceAccess: title=ComfyUI,ssl=false,type=web,port=ui-port
spec:
  type: ClusterIP
  ports:
    - port: 8188
      targetPort: 8188
      name: ui-port
  selector:
    app: comfyui
